# Changelog

## v0.2.1

- Added compact binary snapshot/WAL storage.
- Added legacy JSON auto-migration on load.
- Updated README and docs for the new storage format and `.db` examples.
- Added `v0.2.1` release metadata.

## v0.2.0

- Added Neo4j-compatible Cypher dialect gating and conformance coverage.
- Added guarded support for `UNION`, `UNION ALL`, `CASE`, `EXISTS { ... }`, and `CALL { ... }` subqueries.
- Added compact storage planning and migration groundwork.
- Updated documentation and release metadata.
